import { BellRing, Database, ExternalLink, RefreshCcw, Server } from 'lucide-react';
import { runtimeConfig } from '../../config/runtime';

export function SettingsPage({ pollIntervalMs, showDashboardStatus, onShowDashboardStatusChange }: { pollIntervalMs: number; showDashboardStatus: boolean; onShowDashboardStatusChange: (visible: boolean) => void }) {
  return <section className="settings-cards"><article className="panel info-card"><span><Database size={23} /></span><div><h3>API 資料端點</h3><p>正式環境固定讀取 EC62 Web API。</p><a href={runtimeConfig.apiDataUrl} target="_blank" rel="noreferrer">{runtimeConfig.apiDataUrl}<ExternalLink size={15} /></a></div></article><article className="panel info-card"><span><Server size={23} /></span><div><h3>目前資料模式</h3><p>透過環境設定切換，不修改畫面邏輯。</p><b className={`mode-pill ${runtimeConfig.dataMode}`}>{runtimeConfig.dataMode === 'mock' ? 'MOCK · 模擬資料' : 'LIVE · 正式 API'}</b></div></article><article className="panel info-card"><span><RefreshCcw size={23} /></span><div><h3>更新頻率</h3><p>Dashboard 背景自動重新取得資料。</p><b>{pollIntervalMs / 1000} 秒</b></div></article><article className="panel info-card dashboard-status-setting"><span><BellRing size={23} /></span><div><h3>儀錶板警報狀態列</h3><p>在 Header 顯示警報、斷線或正常狀態。</p><b>{showDashboardStatus ? '目前開啟' : '目前關閉'}</b></div><label className="switch" aria-label="顯示儀錶板警報狀態列"><input type="checkbox" checked={showDashboardStatus} onChange={(event) => onShowDashboardStatusChange(event.target.checked)} /><span /></label></article></section>;
}
